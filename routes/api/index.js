const authRouter = require('./auth-router');
const contactsRouter = require('./contacts-router');
const userRouter = require('./user-router');

module.exports = { authRouter, contactsRouter, userRouter };
